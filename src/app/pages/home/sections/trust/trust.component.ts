import { Component } from '@angular/core';

@Component({
  selector: 'app-trust',
  standalone: true,
  templateUrl: './trust.component.html',
  styleUrls: ['./trust.component.scss'],
})
export class TrustComponent {
  /**
   * Trust section intentionally minimal.
   * No logic required right now.
   *
   * Future use cases:
   * - Scroll-based reveal
   * - IntersectionObserver
   * - Dynamic metrics / counters
   */
}
